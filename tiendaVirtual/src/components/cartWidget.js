import React from 'react';

const CartWidget = () => {
    return (
        <div>
            <img src="carrito.png" alt="Carrito" />
            <span>3</span> {/* Aquí puedes mostrar el número de items en el carrito */}
        </div>
    );
};

export default CartWidget;
